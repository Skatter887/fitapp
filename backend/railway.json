{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cp src/prisma/schema.prod.prisma src/prisma/schema.prisma && npm install --include=dev && npx prisma generate --schema=src/prisma/schema.prisma && npm run build"
  },
  "deploy": {
    "startCommand": "npx prisma db push --schema=src/prisma/schema.prisma --accept-data-loss && node dist/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
